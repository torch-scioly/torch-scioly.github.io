{
  "name": "torch-signups",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "dev": "wrangler pages dev . --d1=DB=torch-signups"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.6.0",
    "vitest": "^2.1.0",
    "wrangler": "^3.90.0"
  }
}
